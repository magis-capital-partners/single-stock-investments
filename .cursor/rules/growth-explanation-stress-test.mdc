---
description: Growth reasoning in Business & moat and assumption ledger — not Popper/Deutsch report blocks
globs: "**/research/**"
alwaysApply: false
---

# Growth explanation (report rules)

Read **`_system/frameworks/growth_explanation_stress_test.md`** — growth is **not** a separate Valuation subsection.

## Required behavior (Marvin)

1. **Primary IRR** — Lawrence `scenarios.base` → `implied_return.base_pct` (executive summary, Classification, returns statement).
2. **Growth mechanism** — Plain English in **Business & moat**; growth ledger rows cite filing or **[Assumption]**.
3. **Bear / bull** — In `valuation.json` scenarios only; one sentence in Payoff & return on what changes vs base.
4. **Optional JSON** — `growth_explanation` in `valuation.json` for scripts; **do not** render in markdown.

## Do not (deep dives)

- Add `### Valuation bridge` overlay rows (theory-implied, falsifier-adjusted, segment sum/implied, Lawrence legacy duplicate).
- Add `### Growth explanation stress test (Popper / Deutsch)` or Deutsch check tables.
- Add weight-scheme falsifiers or "Why these weights" blocks.
- Use "falsifier-adjusted" as primary display label when Lawrence base is the stance gate.

## Milly

Flag orphan growth rows without source. See `MILLY.md` § Growth explanation.
