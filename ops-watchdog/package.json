{
  "name": "ops-watchdog",
  "private": true,
  "type": "module",
  "description": "External dead-man's switch for the repository-health supervisor (Cloudflare Worker, cron every 30 minutes).",
  "scripts": {
    "test": "node --test test/watchdog.test.mjs"
  }
}
