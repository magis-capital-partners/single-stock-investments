name: Drive Intake Sync

on:
  schedule:
    - cron: "20 * * * *"
  workflow_dispatch:
    inputs:
      kind:
        description: "Optional intake kind: vic, research, or company"
        required: false
        type: string
      limit:
        description: "Optional max files to import"
        required: false
        default: "0"
        type: string

permissions:
  contents: write

concurrency:
  group: data-commit-main
  cancel-in-progress: false

jobs:
  import-drive-intake:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          docker-images: true
          swap-storage: true
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            _system/scripts/ci_checkout_workspace.sh
            _system/scripts/ci_resolve_checkout_ref.sh
            _system/scripts/ci_sparse_checkout_paths.py
          sparse-checkout-cone-mode: false
          fetch-depth: 1
      - name: Checkout workspace (full — imports PDFs)
        run: bash _system/scripts/ci_checkout_workspace.sh full

      - name: Check Google credentials secret
        id: google_creds
        env:
          GOOGLE_APPLICATION_CREDENTIALS_JSON: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
        run: |
          if [ -z "$GOOGLE_APPLICATION_CREDENTIALS_JSON" ]; then
            echo "configured=false" >> "$GITHUB_OUTPUT"
            MSG="GOOGLE_APPLICATION_CREDENTIALS_JSON is not configured in Settings → Secrets and variables → Actions."
            DOC="See README.md and _system/reference/document-store/drive_intake_readme.md."
            if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
              echo "::error::${MSG} ${DOC}"
              exit 1
            fi
            echo "::warning::Skipping Drive Intake Sync — ${MSG} ${DOC}"
          else
            echo "configured=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Python
        if: steps.google_creds.outputs.configured == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        if: steps.google_creds.outputs.configured == 'true'
        run: pip install -r _system/scripts/requirements.txt

      - name: Configure Google credentials
        if: steps.google_creds.outputs.configured == 'true'
        env:
          GOOGLE_APPLICATION_CREDENTIALS_JSON: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
        run: |
          printf '%s' "$GOOGLE_APPLICATION_CREDENTIALS_JSON" > "$RUNNER_TEMP/google-service-account.json"
          echo "GOOGLE_APPLICATION_CREDENTIALS=$RUNNER_TEMP/google-service-account.json" >> "$GITHUB_ENV"

      - name: Import Drive intake PDFs
        if: steps.google_creds.outputs.configured == 'true'
        env:
          KIND: ${{ github.event.inputs.kind || '' }}
          LIMIT: ${{ github.event.inputs.limit || '0' }}
        run: |
          ARGS="--ensure-folders"
          if [ -n "$KIND" ]; then ARGS="$ARGS --kind $KIND"; fi
          if [ -n "$LIMIT" ] && [ "$LIMIT" != "0" ]; then ARGS="$ARGS --limit $LIMIT"; fi
          python _system/scripts/import_drive_intake.py $ARGS

      - name: Activist scan (SEC + publishers)
        if: steps.google_creds.outputs.configured == 'true'
        run: |
          python _system/scripts/scan_activist_sources.py --sec-only --reconcile || true
          python _system/scripts/short_scan_batch.py

      - name: Rebuild document registry and dashboard data
        if: steps.google_creds.outputs.configured == 'true'
        uses: ./.github/actions/rebuild-data
        env:
          OAUTH_CLIENT_ID: ${{ vars.OAUTH_CLIENT_ID }}
          OAUTH_PROXY_URL: ${{ vars.OAUTH_PROXY_URL }}
          RESEARCH_VAULT_REPO_URL: ${{ secrets.RESEARCH_VAULT_REPO_URL }}
          RESEARCH_VAULT_CLONE_TOKEN: ${{ secrets.RESEARCH_VAULT_CLONE_TOKEN }}
          RESEARCH_VAULT_ROOT: _external/research-vault
        with:
          profile: intake-full
          mirror_docs: "true"

      - name: Commit imported documents
        if: steps.google_creds.outputs.configured == 'true'
        uses: ./.github/actions/commit-main
        with:
          message: "chore: sync Drive intake documents $(date -u +%Y-%m-%d)"

      - name: Pipeline summary
        if: steps.google_creds.outputs.configured == 'true'
        run: |
          echo "Drive Intake Sync complete. Deploy Dashboard chains via workflow_run."
