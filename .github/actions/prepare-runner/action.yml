name: Prepare runner disk
description: Reclaim disk space on GitHub-hosted ubuntu runners before large checkouts

runs:
  using: composite
  steps:
    - uses: jlumbroso/free-disk-space@main
      with:
        tool-cache: false
        android: true
        dotnet: true
        haskell: true
        docker-images: true
        swap-storage: true
